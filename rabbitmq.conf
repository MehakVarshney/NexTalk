## RabbitMQ Configuration File
## Used by nextalk-rabbitmq container

# Memory threshold - use 40% of available memory
vm_memory_high_watermark.relative = 0.6

# Disk free space limit
disk_free_limit.absolute = 100mb

# Logging
log.console = true
log.console.level = warning
